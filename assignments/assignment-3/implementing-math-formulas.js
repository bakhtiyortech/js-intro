// Implementing Mathematical Formulas
// 1. Declare a variable radius and assign it a value. 
// Calculate the area of a circle using the formula Area = πr² (you can use 3.14 for π), and log the area to the console.

// 2. Declare variables length and width and assign them values. 
// Calculate the perimeter of a rectangle using the formula Perimeter = 2 * (Length + Width), and log the perimeter to the console.

// 3. Declare a variable degrees and assign it a value representing an angle in degrees. 
// Convert this angle to radians and assign the result to a variable radians. The formula for conversion is radians = degrees * (π/180). 
// Log the radians to the console.

// 4. Declare a variable radius and assign it a value. 
// Calculate the perimeter (circumference) of a circle using the formula Perimeter = 2πr, and log the perimeter to the console. 
// Use 3.14 for π.

// 1. Assignment
var radius = 30;
var p = 3.14;
var area = p * radius ** 2;
console.log(area);

// 2. Assignment
var lenght = 15;
var width = 4;
var perimetr = 2 * (lenght + width);
console.log(perimetr);

// 3. Assignment
var degrees = 60;
var p = 3.14;
var radians = degrees * (p / 180);
console.log(radians);

// 4. Assignment
var radius = 60;
var p = 3.14;
var perimetr = 2 * p * radius;
console.log(perimetr);