// .endWith method

// Declare a variable announcement with the value "Today's event starts at 3 PM!".
// Use the endsWith method to check if announcement ends with "PM!". Store the result in a variable and log it.

var announcement = "Today's event starts at 3 PM!";
console.log(announcement.endsWith("PM!")); // true