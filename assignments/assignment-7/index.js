// index[]

// Accessing the Beginning Characters:

// Declare a string variable greeting with the value "Hello, world!".
// Log the first, second, and third characters of the string to the console individually.

// Accessing the Last Characters:

// Explain how to access the last character of the greeting string.
// Log the last character and the one before it (the second-to-last character) to the console.

var greeting = "Hello, world!";
console.log(greeting[0]);
console.log(greeting[1]);
console.log(greeting[2]);
console.log(greeting[greeting.length - 1]);
console.log(greeting[greeting.length - 2]);