// .concat method

// Create two variables, greeting and name, with string values "Hello, " and "Alice" respectively.
// Use the concat method to combine them into a third variable, welcomeMessage.
// Log welcomeMessage to the console.

var greeting = "Hello";
var name = "Alice";

var welcomeMessage = greeting.concat(" " + name);
console.log(welcomeMessage);