// Assignment Operations

// Initialize a new variable total with zero, then add num1 to num2 using the += operator on total. Print total.

// Subtract num1 from total using the -= operator and print total.

// Multiply total by num2 using the *= operator and print total.

// Divide total by num1 using the /= operator and print the final value of total.

var total = 0;
var num1 = 30;
var num2 = 10;

total += (num1 + num2);
console.log(total);

total -= num1;
console.log(total);

total *= num2;
console.log(total);

total /= num1;
console.log(total);